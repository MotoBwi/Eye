interface:
  display_name: "Eye"
  short_description: "Repo-specific patterns and workflows for Eye"
  default_prompt: "Use the Eye repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true